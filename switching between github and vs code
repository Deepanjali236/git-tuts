:: Step 1: Always pull first to get latest changes from GitHub
git pull origin main

:: Step 2: Make new changes locally (if needed), test, and push
git add .
git commit -m "Added section to frontend"
git push origin main
